# Full local Python venv: same packages as the Docker video worker (API + yt-dlp).
# Chain: this file -> requirements-worker.txt -> requirements-api.txt + yt-dlp
#
# Install surfaces:
#   pip install -r requirements.txt          # recommended local dev (one venv)
#   pip install -r requirements-api.txt    # website API only (Dockerfile.api)
#   pip install -r requirements-worker.txt # video worker / Dockerfile.worker
#
# System binaries (local dev, not inside slim API container):
#   ffmpeg + ffprobe on PATH for ingest/render (worker).
#
# ML / clipper prerequisites:
#   This repo's default local install includes faster-whisper + embeddings (see requirements-ml.txt).
#   If you want a lighter install, comment out the requirements-ml include below.
-r requirements-worker.txt
-r requirements-ml.txt
